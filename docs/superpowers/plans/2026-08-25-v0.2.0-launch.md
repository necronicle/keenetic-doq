# v0.2.0 «Выход в свет» — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** CLI-утилита управления апстримами внутри бинарника doqd, установка одной командой, README RU/EN, обвязка публичного репозитория; финал — v0.2.0 и перевод репо в public.

**Architecture:** Один бинарник: `doqd` без аргументов — демон (без изменений), `doqd list|test|add|remove|status` — утилита (новый пакет `internal/cli`, диспетчер в main.go). Конфиг правится построчно с сохранением всех нетронутых строк, живые проверки апстримов — через существующий `upstream.DoQ`. install.sh становится самодостаточным (heredoc-шаблоны) и переезжает в корень.

**Tech Stack:** Go 1.23, quic-go v0.48.2, miekg/dns v1.1.62, POSIX sh, GitHub Actions.

**Spec:** `docs/superpowers/specs/2026-08-25-v0.2.0-launch-design.md`

## Global Constraints

- go.mod и CI: go **1.23** (не поднимать).
- Вывод CLI-утилиты и новых скриптов — **английский**; README — RU (основной) + EN.
- Порт 53 и ndnproxy не трогаются никогда; дефолтный порт doqd — **5354**.
- KeeneticOS не принимает loopback в `ip name-server` — регистрация только LAN-IP.
- Лицензия — **GPL-3.0**. Репозиторий переводится в public только после явного подтверждения пользователя (Task 12).
- Тест-роутер: `sshpass -p '<пароль из /Users/mark/.claude/projects/-Library-Zapret2/memory/reference_router_ssh.md>' ssh -o StrictHostKeyChecking=no -p 222 root@192.168.1.1`; scp с флагами `-O -P 222`. На роутере нет `nohup`/`timeout`; `killall doqd` убивает продакшен-демона — не использовать.
- Коммиты завершать `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Диспетчер подкоманд

**Files:**
- Create: `internal/cli/cli.go`
- Test: `internal/cli/cli_test.go`
- Modify: `cmd/doqd/main.go` (вставка перед `flag.String`)

**Interfaces:**
- Consumes: ничего из других задач.
- Produces: `cli.Run(args []string) int`, `cli.IsSubcommand(s string) bool`, константа `defaultConf = "/opt/etc/doqd.conf"`. Подкоманды регистрируются в `var subcommands map[string]func([]string) int` — задачи 3, 5, 6, 7 добавляют в неё свои записи. До их появления карта содержит только заглушку `help`.

- [ ] **Step 1: Написать падающий тест**

`internal/cli/cli_test.go`:

```go
package cli

import "testing"

func TestRunUnknownCommand(t *testing.T) {
	if got := Run([]string{"frobnicate"}); got != 2 {
		t.Fatalf("Run(unknown) = %d, want 2", got)
	}
}

func TestIsSubcommand(t *testing.T) {
	if IsSubcommand("frobnicate") {
		t.Fatal("frobnicate must not be a subcommand")
	}
	if !IsSubcommand("help") {
		t.Fatal("help must be a subcommand")
	}
}
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `go test ./internal/cli/`
Expected: FAIL — пакет не существует / `Run` не определён.

- [ ] **Step 3: Минимальная реализация**

`internal/cli/cli.go`:

```go
// Package cli — режим утилиты управления doqd: подкоманды
// list/test/add/remove/status поверх того же бинарника, что и демон.
package cli

import (
	"fmt"
	"os"
)

const defaultConf = "/opt/etc/doqd.conf"

var subcommands = map[string]func(args []string) int{
	"help": func([]string) int { usage(os.Stdout); return 0 },
}

func IsSubcommand(s string) bool {
	_, ok := subcommands[s]
	return ok
}

func Run(args []string) int {
	if len(args) == 0 {
		usage(os.Stderr)
		return 2
	}
	cmd, ok := subcommands[args[0]]
	if !ok {
		fmt.Fprintf(os.Stderr, "unknown command %q\n\n", args[0])
		usage(os.Stderr)
		return 2
	}
	return cmd(args[1:])
}

func usage(w *os.File) {
	fmt.Fprint(w, `doqd — DNS-over-QUIC forwarder for Keenetic routers

Daemon mode (used by the init script):
  doqd [-c /opt/etc/doqd.conf]

Management commands:
  doqd list                       upstreams from the config with live probes
  doqd test quic://host[:port]    probe any DoQ server, config untouched
  doqd add [--force] quic://...   probe, add to config, restart the daemon
  doqd remove <number|url>        remove an upstream, restart the daemon
  doqd status                     daemon, registration and resolve check

Flags accepted by management commands:
  -c path    config file (default /opt/etc/doqd.conf)
`)
}
```

- [ ] **Step 4: Прогнать тест**

Run: `go test ./internal/cli/`
Expected: PASS

- [ ] **Step 5: Подключить в main.go**

В `cmd/doqd/main.go` первой строкой тела `main()` (до объявления флагов):

```go
	if len(os.Args) > 1 && cli.IsSubcommand(os.Args[1]) {
		os.Exit(cli.Run(os.Args[1:]))
	}
```

и импорт `"github.com/necronicle/keenetic-doq/internal/cli"`.

- [ ] **Step 6: Проверка сборки и поведения**

Run: `go build ./... && go vet ./... && go run ./cmd/doqd help`
Expected: сборка чистая, `help` печатает usage и завершается с кодом 0; `go run ./cmd/doqd -version` по-прежнему печатает версию.

- [ ] **Step 7: Commit**

```bash
git add internal/cli/ cmd/doqd/main.go
git commit -m "feat: subcommand dispatcher for doqd management CLI"
```

---

### Task 2: Построчное редактирование конфига

**Files:**
- Create: `internal/cli/confedit.go`
- Test: `internal/cli/confedit_test.go`

**Interfaces:**
- Consumes: `config.Default()` (`internal/config`).
- Produces (используют задачи 5–7):
  - `readConfLines(path string) (lines []string, exists bool, err error)`
  - `confUpstreams(lines []string) []string`
  - `confListen(lines []string) string` — значение `listen` или дефолт из `config.Default().Listen`
  - `addUpstream(lines []string, url string) ([]string, error)` — дубликат = ошибка
  - `removeUpstream(lines []string, sel string) (out []string, removed string, err error)` — sel = 1-based номер или URL; последний апстрим удалить нельзя
  - `writeConfLines(path string, lines []string) error` — атомарно (tmp+rename)
  - `defaultConfLines() []string` — дефолтный конфиг как строки

- [ ] **Step 1: Написать падающие тесты**

`internal/cli/confedit_test.go`:

```go
package cli

import (
	"os"
	"path/filepath"
	"reflect"
	"strings"
	"testing"

	"github.com/necronicle/keenetic-doq/internal/config"
)

var sample = []string{
	"# doqd config",
	"listen 192.168.1.1:5354",
	"",
	"upstream quic://dns.comss.one",
	"upstream quic://unfiltered.adguard-dns.com",
	"cache_size 4096",
	"log info",
}

func TestConfUpstreamsAndListen(t *testing.T) {
	got := confUpstreams(sample)
	want := []string{"quic://dns.comss.one", "quic://unfiltered.adguard-dns.com"}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("confUpstreams = %v, want %v", got, want)
	}
	if l := confListen(sample); l != "192.168.1.1:5354" {
		t.Fatalf("confListen = %q", l)
	}
	if l := confListen([]string{"# empty"}); l != config.Default().Listen {
		t.Fatalf("confListen fallback = %q", l)
	}
}

func TestAddUpstream(t *testing.T) {
	out, err := addUpstream(sample, "quic://dns.quad9.net")
	if err != nil {
		t.Fatal(err)
	}
	// вставка сразу после последней upstream-строки, остальное нетронуто
	if out[5] != "upstream quic://dns.quad9.net" || out[6] != "cache_size 4096" {
		t.Fatalf("wrong insertion: %v", out)
	}
	if out[0] != "# doqd config" || len(out) != len(sample)+1 {
		t.Fatalf("other lines must be preserved: %v", out)
	}
	if _, err := addUpstream(sample, "quic://dns.comss.one"); err == nil {
		t.Fatal("duplicate must be rejected")
	}
}

func TestAddUpstreamNoExisting(t *testing.T) {
	out, err := addUpstream([]string{"listen 1.2.3.4:5354"}, "quic://a.example")
	if err != nil || out[len(out)-1] != "upstream quic://a.example" {
		t.Fatalf("append to end: %v, %v", out, err)
	}
}

func TestRemoveUpstream(t *testing.T) {
	out, removed, err := removeUpstream(sample, "2")
	if err != nil || removed != "quic://unfiltered.adguard-dns.com" {
		t.Fatalf("remove by number: %q, %v", removed, err)
	}
	if len(confUpstreams(out)) != 1 {
		t.Fatalf("one upstream must remain: %v", out)
	}
	if _, _, err := removeUpstream(sample, "quic://dns.comss.one"); err != nil {
		t.Fatalf("remove by url: %v", err)
	}
	if _, _, err := removeUpstream(sample, "9"); err == nil {
		t.Fatal("out-of-range number must fail")
	}
	if _, _, err := removeUpstream(sample, "quic://nope.example"); err == nil {
		t.Fatal("unknown url must fail")
	}
	one := []string{"upstream quic://dns.comss.one"}
	if _, _, err := removeUpstream(one, "1"); err == nil {
		t.Fatal("last upstream must be protected")
	}
}

func TestReadWriteRoundTrip(t *testing.T) {
	path := filepath.Join(t.TempDir(), "doqd.conf")
	if _, exists, err := readConfLines(path); err != nil || exists {
		t.Fatalf("missing file: exists=%v err=%v", exists, err)
	}
	if err := writeConfLines(path, sample); err != nil {
		t.Fatal(err)
	}
	lines, exists, err := readConfLines(path)
	if err != nil || !exists || !reflect.DeepEqual(lines, sample) {
		t.Fatalf("round trip: %v %v %v", lines, exists, err)
	}
	raw, _ := os.ReadFile(path)
	if !strings.HasSuffix(string(raw), "\n") {
		t.Fatal("file must end with newline")
	}
}

func TestDefaultConfLinesParse(t *testing.T) {
	cfg, err := config.Parse(strings.NewReader(strings.Join(defaultConfLines(), "\n")))
	if err != nil {
		t.Fatalf("defaults must parse: %v", err)
	}
	if !reflect.DeepEqual(cfg, config.Default()) {
		t.Fatalf("defaults mismatch: %+v vs %+v", cfg, config.Default())
	}
}
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `go test ./internal/cli/`
Expected: FAIL — функции не определены.

- [ ] **Step 3: Реализация**

`internal/cli/confedit.go`:

```go
package cli

import (
	"bufio"
	"fmt"
	"os"
	"path/filepath"
	"strconv"
	"strings"

	"github.com/necronicle/keenetic-doq/internal/config"
)

// readConfLines читает конфиг построчно; отсутствие файла — не ошибка.
func readConfLines(path string) ([]string, bool, error) {
	f, err := os.Open(path)
	if os.IsNotExist(err) {
		return nil, false, nil
	}
	if err != nil {
		return nil, false, err
	}
	defer f.Close()
	var lines []string
	sc := bufio.NewScanner(f)
	for sc.Scan() {
		lines = append(lines, sc.Text())
	}
	return lines, true, sc.Err()
}

// isUpstreamLine распознаёт строку "upstream <url>" по тем же правилам,
// что и config.Parse (первые два поля).
func isUpstreamLine(line string) (string, bool) {
	fields := strings.Fields(line)
	if len(fields) >= 2 && fields[0] == "upstream" {
		return fields[1], true
	}
	return "", false
}

func confUpstreams(lines []string) []string {
	var ups []string
	for _, l := range lines {
		if u, ok := isUpstreamLine(l); ok {
			ups = append(ups, u)
		}
	}
	return ups
}

func confListen(lines []string) string {
	for _, l := range lines {
		fields := strings.Fields(l)
		if len(fields) >= 2 && fields[0] == "listen" {
			return fields[1]
		}
	}
	return config.Default().Listen
}

func addUpstream(lines []string, url string) ([]string, error) {
	last := -1
	for i, l := range lines {
		u, ok := isUpstreamLine(l)
		if !ok {
			continue
		}
		if u == url {
			return nil, fmt.Errorf("upstream %s is already in the config", url)
		}
		last = i
	}
	entry := "upstream " + url
	out := make([]string, 0, len(lines)+1)
	if last == -1 {
		out = append(out, lines...)
		return append(out, entry), nil
	}
	out = append(out, lines[:last+1]...)
	out = append(out, entry)
	return append(out, lines[last+1:]...), nil
}

func removeUpstream(lines []string, sel string) ([]string, string, error) {
	ups := confUpstreams(lines)
	if len(ups) == 0 {
		return nil, "", fmt.Errorf("no upstreams in the config")
	}
	target := sel
	if n, err := strconv.Atoi(sel); err == nil {
		if n < 1 || n > len(ups) {
			return nil, "", fmt.Errorf("no upstream #%d (config has %d)", n, len(ups))
		}
		target = ups[n-1]
	}
	found := false
	for _, u := range ups {
		if u == target {
			found = true
		}
	}
	if !found {
		return nil, "", fmt.Errorf("upstream %s not found in the config", target)
	}
	if len(ups) == 1 {
		return nil, "", fmt.Errorf("refusing to remove the last upstream — add another one first: doqd add quic://...")
	}
	var out []string
	removed := false
	for _, l := range lines {
		if u, ok := isUpstreamLine(l); ok && u == target && !removed {
			removed = true
			continue
		}
		out = append(out, l)
	}
	return out, target, nil
}

// writeConfLines атомарно перезаписывает конфиг: tmp-файл в том же
// каталоге + rename, права 0644.
func writeConfLines(path string, lines []string) error {
	tmp, err := os.CreateTemp(filepath.Dir(path), ".doqd.conf.*")
	if err != nil {
		return err
	}
	defer os.Remove(tmp.Name())
	for _, l := range lines {
		if _, err := fmt.Fprintln(tmp, l); err != nil {
			tmp.Close()
			return err
		}
	}
	if err := tmp.Close(); err != nil {
		return err
	}
	if err := os.Chmod(tmp.Name(), 0o644); err != nil {
		return err
	}
	return os.Rename(tmp.Name(), path)
}

func defaultConfLines() []string {
	def := config.Default()
	lines := []string{
		"# doqd — DNS-over-QUIC forwarder. https://github.com/necronicle/keenetic-doq",
		"listen " + def.Listen,
	}
	for _, u := range def.Upstreams {
		lines = append(lines, "upstream "+u)
	}
	return append(lines,
		fmt.Sprintf("cache_size %d", def.CacheSize),
		fmt.Sprintf("min_ttl %d", int(def.MinTTL.Seconds())),
		fmt.Sprintf("max_ttl %d", int(def.MaxTTL.Seconds())),
		"log "+def.LogLevel)
}
```

- [ ] **Step 4: Прогнать тесты**

Run: `go test ./internal/cli/ && go vet ./...`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/cli/confedit.go internal/cli/confedit_test.go
git commit -m "feat: line-preserving config editing for the management CLI"
```

---

### Task 3: Живая проверка и подкоманда test

**Files:**
- Create: `internal/cli/probe.go`
- Modify: `internal/cli/cli.go` (регистрация в `subcommands`)
- Test: `internal/cli/probe_test.go`

**Interfaces:**
- Consumes: `upstream.NewDoQ`, `(*upstream.DoQ).Exchange/Close`.
- Produces (используют задачи 5, 6): `probe(rawURL string) probeResult`, тип `probeResult{RTT time.Duration; Err error}`, константы `probeTimeout = 5 * time.Second`, `probeName = "keenetic.com."`, функция `runTest(args []string) int`.

- [ ] **Step 1: Написать падающий тест**

`internal/cli/probe_test.go`:

```go
package cli

import "testing"

func TestProbeBadURL(t *testing.T) {
	if r := probe("https://not-quic.example"); r.Err == nil {
		t.Fatal("non-quic scheme must fail")
	}
}

func TestRunTestUsage(t *testing.T) {
	if got := Run([]string{"test"}); got != 2 {
		t.Fatalf("test without args = %d, want 2", got)
	}
}
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `go test ./internal/cli/`
Expected: FAIL — `probe` не определён, подкоманда `test` не зарегистрирована (Run вернёт 2 и второй тест «пройдёт» случайно — это нормально, договор фиксируется первым тестом).

- [ ] **Step 3: Реализация**

`internal/cli/probe.go`:

```go
package cli

import (
	"context"
	"flag"
	"fmt"
	"os"
	"time"

	"github.com/miekg/dns"

	"github.com/necronicle/keenetic-doq/internal/upstream"
)

const (
	probeTimeout = 5 * time.Second
	probeName    = "keenetic.com."
)

type probeResult struct {
	RTT time.Duration
	Err error
}

// probe делает один живой DoQ-запрос к серверу и меряет RTT.
func probe(rawURL string) probeResult {
	u, err := upstream.NewDoQ(rawURL)
	if err != nil {
		return probeResult{Err: err}
	}
	defer u.Close()
	ctx, cancel := context.WithTimeout(context.Background(), probeTimeout)
	defer cancel()
	m := new(dns.Msg)
	m.SetQuestion(probeName, dns.TypeA)
	start := time.Now()
	if _, err := u.Exchange(ctx, m); err != nil {
		return probeResult{Err: err}
	}
	return probeResult{RTT: time.Since(start)}
}

func runTest(args []string) int {
	fs := flag.NewFlagSet("doqd test", flag.ExitOnError)
	fs.Parse(args)
	if fs.NArg() != 1 {
		fmt.Fprintln(os.Stderr, "usage: doqd test quic://host[:port]")
		return 2
	}
	url := fs.Arg(0)
	if _, err := upstream.NewDoQ(url); err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	fmt.Printf("probing %s ... ", url)
	r := probe(url)
	if r.Err != nil {
		fmt.Println("FAIL")
		fmt.Fprintln(os.Stderr, "error:", r.Err)
		return 1
	}
	fmt.Printf("OK — answered in %d ms\n", r.RTT.Milliseconds())
	return 0
}
```

В `internal/cli/cli.go` добавить в карту `subcommands` строку:

```go
	"test": runTest,
```

- [ ] **Step 4: Прогнать тесты и живую проверку с мака**

Run: `go test ./internal/cli/ && go run ./cmd/doqd test quic://dns.comss.one`
Expected: тесты PASS; живой вызов печатает `OK — answered in NN ms`, код выхода 0. Затем `go run ./cmd/doqd test quic://127.0.0.1:9`: `FAIL` + ошибка, код 1 (ждать до 5 секунд).

- [ ] **Step 5: Commit**

```bash
git add internal/cli/probe.go internal/cli/probe_test.go internal/cli/cli.go
git commit -m "feat: doqd test — live DoQ probe of any server"
```

---

### Task 4: Хелперы демона (restart, pid, uptime, ndm)

**Files:**
- Create: `internal/cli/daemon.go`

**Interfaces:**
- Consumes: ничего из других задач.
- Produces (используют задачи 5–7): `restartDaemon() error` (печатает прогресс, вне роутера — предупреждение и nil), `daemonPID() int` (0 = не найден/не Linux), `daemonUptime(pid int) (time.Duration, bool)`, `ndmShow(cmd string) (out string, available bool)`, константа `initScript = "/opt/etc/init.d/S56doqd"`.

Юнит-тестов нет: всё завязано на exec/procfs; проверяется живым тестом на роутере (Task 11). Компиляция и vet — обязательны.

- [ ] **Step 1: Реализация**

`internal/cli/daemon.go`:

```go
package cli

import (
	"bytes"
	"fmt"
	"os"
	"os/exec"
	"strconv"
	"strings"
	"time"
)

const initScript = "/opt/etc/init.d/S56doqd"

// restartDaemon перезапускает демона через init-скрипт Entware и ждёт
// подтверждения alive. Вне роутера (скрипта нет) — предупреждение, nil.
func restartDaemon() error {
	if _, err := os.Stat(initScript); err != nil {
		fmt.Println("restart skipped: init script not found (not on the router?)")
		return nil
	}
	fmt.Print("restarting the daemon ... ")
	if out, err := exec.Command(initScript, "restart").CombinedOutput(); err != nil {
		fmt.Println("FAIL")
		return fmt.Errorf("restart failed: %v\n%s", err, out)
	}
	time.Sleep(time.Second)
	out, err := exec.Command(initScript, "check").CombinedOutput()
	if err == nil && strings.Contains(string(out), "alive") {
		fmt.Println("alive")
		return nil
	}
	fmt.Println("FAIL")
	return fmt.Errorf("daemon did not come back up (%s check: %s)", initScript, strings.TrimSpace(string(out)))
}

// daemonPID ищет работающий doqd по /proc/*/comm, пропуская себя.
func daemonPID() int {
	self := os.Getpid()
	entries, err := os.ReadDir("/proc")
	if err != nil {
		return 0 // не Linux — статус демона недоступен
	}
	for _, e := range entries {
		pid, err := strconv.Atoi(e.Name())
		if err != nil || pid == self {
			continue
		}
		comm, err := os.ReadFile("/proc/" + e.Name() + "/comm")
		if err != nil {
			continue
		}
		if strings.TrimSpace(string(comm)) == "doqd" {
			return pid
		}
	}
	return 0
}

// daemonUptime считает аптайм процесса по starttime из /proc/<pid>/stat
// (поле 22) и /proc/uptime. CLK_TCK на Linux — 100.
func daemonUptime(pid int) (time.Duration, bool) {
	stat, err := os.ReadFile(fmt.Sprintf("/proc/%d/stat", pid))
	if err != nil {
		return 0, false
	}
	i := bytes.LastIndexByte(stat, ')') // имя процесса в скобках может содержать пробелы
	if i < 0 {
		return 0, false
	}
	fields := strings.Fields(string(stat[i+1:]))
	if len(fields) < 20 {
		return 0, false
	}
	ticks, err := strconv.ParseFloat(fields[19], 64)
	if err != nil {
		return 0, false
	}
	up, err := os.ReadFile("/proc/uptime")
	if err != nil {
		return 0, false
	}
	var sys float64
	if _, err := fmt.Sscanf(string(up), "%f", &sys); err != nil {
		return 0, false
	}
	const clkTck = 100
	return time.Duration(sys-ticks/clkTck) * time.Second, true
}

// ndmShow выполняет команду CLI KeeneticOS через ndmc (или старый ndmq).
func ndmShow(cmd string) (string, bool) {
	if p, err := exec.LookPath("ndmc"); err == nil {
		out, _ := exec.Command(p, "-c", cmd).CombinedOutput()
		return string(out), true
	}
	if p, err := exec.LookPath("ndmq"); err == nil {
		out, _ := exec.Command(p, "-p", cmd).CombinedOutput()
		return string(out), true
	}
	return "", false
}
```

- [ ] **Step 2: Проверка сборки**

Run: `go build ./... && go vet ./... && go test ./...`
Expected: всё чисто (daemon.go пока не используется — если vet ругнётся на неиспользуемое, это нормально только для незакоммиченного кода; неиспользуемые ФУНКЦИИ пакета vet не трогает, ошибки быть не должно).

- [ ] **Step 3: Commit**

```bash
git add internal/cli/daemon.go
git commit -m "feat: daemon helpers — init-script restart, pid/uptime, ndm CLI access"
```

---

### Task 5: Подкоманда list

**Files:**
- Create: `internal/cli/list.go`
- Modify: `internal/cli/cli.go` (регистрация)

**Interfaces:**
- Consumes: `readConfLines`, `confUpstreams`, `confListen` (Task 2); `probe`, `probeResult` (Task 3); `daemonPID` (Task 4); `config.Default()`.
- Produces: `runList(args []string) int`.

- [ ] **Step 1: Реализация**

`internal/cli/list.go`:

```go
package cli

import (
	"flag"
	"fmt"
	"os"
	"sync"

	"github.com/necronicle/keenetic-doq/internal/config"
)

func runList(args []string) int {
	fs := flag.NewFlagSet("doqd list", flag.ExitOnError)
	conf := fs.String("c", defaultConf, "path to config file")
	fs.Parse(args)

	lines, exists, err := readConfLines(*conf)
	if err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	ups, listen := confUpstreams(lines), confListen(lines)
	if !exists {
		def := config.Default()
		ups, listen = def.Upstreams, def.Listen
		fmt.Printf("config %s not found — showing built-in defaults\n\n", *conf)
	}

	results := make([]probeResult, len(ups))
	var wg sync.WaitGroup
	for i, u := range ups {
		wg.Add(1)
		go func(i int, u string) {
			defer wg.Done()
			results[i] = probe(u)
		}(i, u)
	}
	wg.Wait()

	fmt.Printf("UPSTREAMS (%s):\n", *conf)
	for i, u := range ups {
		if results[i].Err != nil {
			fmt.Printf(" %d. %-42s down   (%v)\n", i+1, u, results[i].Err)
		} else {
			fmt.Printf(" %d. %-42s alive  rtt %d ms\n", i+1, u, results[i].RTT.Milliseconds())
		}
	}

	state := "not running"
	if pid := daemonPID(); pid > 0 {
		state = fmt.Sprintf("running (pid %d)", pid)
	}
	fmt.Printf("\nlisten: %s   daemon: %s\n", listen, state)
	return 0
}
```

В `internal/cli/cli.go` в карту `subcommands`:

```go
	"list": runList,
```

- [ ] **Step 2: Проверка с мака**

Run: `go test ./... && go run ./cmd/doqd list -c deploy/doqd.conf`
Expected: тесты PASS; список из двух апстримов, comss `alive` с RTT, AdGuard скорее всего `down` (ТСПУ — ждать до 5 с), внизу `listen: 127.0.0.1:5354  daemon: not running`.

- [ ] **Step 3: Commit**

```bash
git add internal/cli/list.go internal/cli/cli.go
git commit -m "feat: doqd list — upstreams with live probes and daemon state"
```

---

### Task 6: Подкоманды add и remove

**Files:**
- Create: `internal/cli/edit_commands.go`
- Modify: `internal/cli/cli.go` (регистрация)
- Test: `internal/cli/edit_commands_test.go`

**Interfaces:**
- Consumes: `readConfLines`, `addUpstream`, `removeUpstream`, `writeConfLines`, `confUpstreams`, `defaultConfLines` (Task 2); `probe` (Task 3); `restartDaemon` (Task 4); `upstream.NewDoQ`.
- Produces: `runAdd(args []string) int`, `runRemove(args []string) int`.

- [ ] **Step 1: Написать падающий тест**

`internal/cli/edit_commands_test.go` (сеть не нужна: кривой URL режется валидацией, remove работает по файлу):

```go
package cli

import (
	"os"
	"path/filepath"
	"strings"
	"testing"
)

func writeSample(t *testing.T) string {
	t.Helper()
	path := filepath.Join(t.TempDir(), "doqd.conf")
	if err := writeConfLines(path, sample); err != nil {
		t.Fatal(err)
	}
	return path
}

func TestRunAddRejectsBadURL(t *testing.T) {
	path := writeSample(t)
	if got := Run([]string{"add", "-c", path, "https://wrong.example"}); got != 1 {
		t.Fatalf("add bad url = %d, want 1", got)
	}
	lines, _, _ := readConfLines(path)
	if len(confUpstreams(lines)) != 2 {
		t.Fatal("config must be untouched")
	}
}

func TestRunRemoveByNumber(t *testing.T) {
	path := writeSample(t)
	if got := Run([]string{"remove", "-c", path, "2"}); got != 0 {
		t.Fatalf("remove = %d, want 0", got)
	}
	lines, _, _ := readConfLines(path)
	ups := confUpstreams(lines)
	if len(ups) != 1 || ups[0] != "quic://dns.comss.one" {
		t.Fatalf("wrong config after remove: %v", ups)
	}
}

func TestRunRemoveLastRefused(t *testing.T) {
	path := filepath.Join(t.TempDir(), "doqd.conf")
	if err := writeConfLines(path, []string{"upstream quic://dns.comss.one"}); err != nil {
		t.Fatal(err)
	}
	if got := Run([]string{"remove", "-c", path, "1"}); got != 1 {
		t.Fatalf("remove last = %d, want 1", got)
	}
	raw, _ := os.ReadFile(path)
	if !strings.Contains(string(raw), "quic://dns.comss.one") {
		t.Fatal("config must be untouched")
	}
}
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `go test ./internal/cli/`
Expected: FAIL — подкоманды `add`/`remove` не зарегистрированы (Run вернёт 2).

- [ ] **Step 3: Реализация**

`internal/cli/edit_commands.go`:

```go
package cli

import (
	"flag"
	"fmt"
	"os"

	"github.com/necronicle/keenetic-doq/internal/upstream"
)

func runAdd(args []string) int {
	fs := flag.NewFlagSet("doqd add", flag.ExitOnError)
	conf := fs.String("c", defaultConf, "path to config file")
	force := fs.Bool("force", false, "add even if the live probe fails")
	fs.Parse(args)
	if fs.NArg() != 1 {
		fmt.Fprintln(os.Stderr, "usage: doqd add [--force] quic://host[:port]")
		return 2
	}
	url := fs.Arg(0)
	if _, err := upstream.NewDoQ(url); err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}

	fmt.Printf("probing %s ... ", url)
	if r := probe(url); r.Err != nil {
		fmt.Println("FAIL")
		if !*force {
			fmt.Fprintf(os.Stderr, "error: %v\nnot added — re-run with --force to add anyway\n", r.Err)
			return 1
		}
		fmt.Println("adding anyway (--force)")
	} else {
		fmt.Printf("OK (%d ms)\n", r.RTT.Milliseconds())
	}

	lines, exists, err := readConfLines(*conf)
	if err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	if !exists {
		lines = defaultConfLines()
		fmt.Printf("config %s not found — creating it with defaults, review the listen address\n", *conf)
	}
	lines, err = addUpstream(lines, url)
	if err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	if err := writeConfLines(*conf, lines); err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	fmt.Printf("added to %s (upstream #%d)\n", *conf, len(confUpstreams(lines)))
	if err := restartDaemon(); err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	return 0
}

func runRemove(args []string) int {
	fs := flag.NewFlagSet("doqd remove", flag.ExitOnError)
	conf := fs.String("c", defaultConf, "path to config file")
	fs.Parse(args)
	if fs.NArg() != 1 {
		fmt.Fprintln(os.Stderr, "usage: doqd remove <number|quic://url>  (numbers as shown by: doqd list)")
		return 2
	}
	lines, exists, err := readConfLines(*conf)
	if err == nil && !exists {
		err = fmt.Errorf("config %s not found", *conf)
	}
	if err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	lines, removed, err := removeUpstream(lines, fs.Arg(0))
	if err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	if err := writeConfLines(*conf, lines); err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	fmt.Printf("removed %s\n", removed)
	if err := restartDaemon(); err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	return 0
}
```

В `internal/cli/cli.go` в карту `subcommands`:

```go
	"add":    runAdd,
	"remove": runRemove,
```

- [ ] **Step 4: Прогнать тесты**

Run: `go test ./internal/cli/ && go vet ./...`
Expected: PASS (remove-тесты печатают «restart skipped…» — это норма вне роутера).

- [ ] **Step 5: Живая проверка add с мака**

```bash
cp deploy/doqd.conf /tmp/doqd-add-test.conf
go run ./cmd/doqd add -c /tmp/doqd-add-test.conf quic://dns.quad9.net
grep -c '^upstream' /tmp/doqd-add-test.conf   # ожидается 3
rm /tmp/doqd-add-test.conf
```

Expected: проба OK (~200 мс), `added to ... (upstream #3)`, `restart skipped: init script not found`, в файле три upstream-строки, комментарии не тронуты.

- [ ] **Step 6: Commit**

```bash
git add internal/cli/edit_commands.go internal/cli/edit_commands_test.go internal/cli/cli.go
git commit -m "feat: doqd add/remove — probe-gated upstream editing with daemon restart"
```

---

### Task 7: Подкоманда status

**Files:**
- Create: `internal/cli/status.go`
- Modify: `internal/cli/cli.go` (регистрация)

**Interfaces:**
- Consumes: `readConfLines`, `confListen` (Task 2); `probeName`, `probeTimeout` (Task 3); `daemonPID`, `daemonUptime`, `ndmShow` (Task 4); `config.Default()`; `miekg/dns` клиент.
- Produces: `runStatus(args []string) int`. Каждая строка вывода независима, код выхода всегда 0 (кроме нечитаемого конфига — 1).

- [ ] **Step 1: Реализация**

`internal/cli/status.go`:

```go
package cli

import (
	"flag"
	"fmt"
	"net"
	"os"
	"strings"
	"time"

	"github.com/miekg/dns"
)

func runStatus(args []string) int {
	fs := flag.NewFlagSet("doqd status", flag.ExitOnError)
	conf := fs.String("c", defaultConf, "path to config file")
	fs.Parse(args)

	lines, _, err := readConfLines(*conf)
	if err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		return 1
	}
	listen := confListen(lines)

	if pid := daemonPID(); pid > 0 {
		if up, ok := daemonUptime(pid); ok {
			fmt.Printf("daemon:          running (pid %d, uptime %s)\n", pid, up.Round(time.Second))
		} else {
			fmt.Printf("daemon:          running (pid %d)\n", pid)
		}
	} else {
		fmt.Println("daemon:          not running")
	}
	fmt.Printf("listen:          %s (udp+tcp)\n", listen)

	host, port, err := net.SplitHostPort(listen)
	if err != nil {
		host, port = listen, ""
	}
	if out, ok := ndmShow("show ip name-server"); !ok {
		fmt.Println("registration:    unknown (ndmc/ndmq not found — not on the router?)")
	} else if strings.Contains(out, host) && (port == "" || strings.Contains(out, port)) {
		fmt.Println("registration:    present in KeeneticOS name-servers")
	} else {
		fmt.Printf("registration:    NOT found — run: ndmc -c 'ip name-server %s'\n", listen)
	}

	m := new(dns.Msg)
	m.SetQuestion(probeName, dns.TypeA)
	c := &dns.Client{Timeout: probeTimeout}
	start := time.Now()
	resp, _, err := c.Exchange(m, net.JoinHostPort(host, "53"))
	switch {
	case err != nil:
		fmt.Printf("resolve via :53: FAIL (%v)\n", err)
	case resp.Rcode != dns.RcodeSuccess:
		fmt.Printf("resolve via :53: %s\n", dns.RcodeToString[resp.Rcode])
	default:
		fmt.Printf("resolve via :53: NOERROR, %d ms\n", time.Since(start).Milliseconds())
	}
	return 0
}
```

В `internal/cli/cli.go` в карту `subcommands`:

```go
	"status": runStatus,
```

- [ ] **Step 2: Проверка с мака**

Run: `go test ./... && go vet ./... && go run ./cmd/doqd status -c deploy/doqd.conf`
Expected: тесты PASS; вывод: `daemon: not running` (на маке /proc нет), `listen: 127.0.0.1:5354`, `registration: unknown (...)`, resolve-строка (FAIL на маке — норма, :53 закрыт).

- [ ] **Step 3: Commit**

```bash
git add internal/cli/status.go internal/cli/cli.go
git commit -m "feat: doqd status — daemon, registration and end-to-end resolve check"
```

---

### Task 8: Самодостаточные install.sh/uninstall.sh в корне + SHA256SUMS в релизе

**Files:**
- Create: `install.sh` (корень), `uninstall.sh` (корень)
- Modify: `.github/workflows/release.yml`
- Delete: `deploy/install.sh`, `deploy/uninstall.sh`, `deploy/doqd.conf`, `deploy/S56doqd` (каталог `deploy/` целиком)

**Interfaces:**
- Consumes: `doqd test <url>` не используется инсталлером (установка идёт до появления привычки, health виден через `doqd status`); формат ассетов релиза `doqd-linux-{arm64,mipsle,mips}` + `SHA256SUMS`.
- Produces: установка одной командой `curl -fsSL https://raw.githubusercontent.com/necronicle/keenetic-doq/main/install.sh | sh`; режим `sh install.sh --local <binary>` сохраняется.

- [ ] **Step 1: Написать новый install.sh в корне**

```sh
#!/bin/sh
# keenetic-doq installer. Run ON THE ROUTER (Entware required).
#
#   curl -fsSL https://raw.githubusercontent.com/necronicle/keenetic-doq/main/install.sh | sh
#   sh install.sh --local ./doqd-linux-arm64    # offline install
#
# What it does: detects the arch, installs /opt/sbin/doqd, writes
# /opt/etc/doqd.conf (with the router's LAN address) and the Entware
# init script, starts the daemon and registers it as an extra system
# name-server (ip name-server <LAN-IP>:5354). Port 53 is never touched.
set -e

REPO="necronicle/keenetic-doq"
BIN=/opt/sbin/doqd
CONF=/opt/etc/doqd.conf
INIT=/opt/etc/init.d/S56doqd
PORT=5354

log() { echo "[keenetic-doq] $*"; }
die() { echo "[keenetic-doq] ERROR: $*" >&2; exit 1; }

# Keenetic CLI: ndmc (current) or ndmq (legacy)
ndm_cmd() {
    if command -v ndmc >/dev/null 2>&1; then
        ndmc -c "$1"
    elif command -v ndmq >/dev/null 2>&1; then
        ndmq -p "$1"
    else
        die "ndmc/ndmq not found — run manually in the router CLI: $1"
    fi
}

fetch() { # $1 = url, $2 = output file
    if [ -n "$GITHUB_TOKEN" ]; then
        curl -fsSL -H "Authorization: Bearer $GITHUB_TOKEN" -o "$2" "$1"
    else
        curl -fsSL -o "$2" "$1"
    fi
}

[ -f /opt/etc/init.d/rc.func ] || die "Entware not found (/opt/etc/init.d/rc.func)"

# KeeneticOS rejects 127.0.0.1 in `ip name-server`, so doqd listens on
# the router's LAN address (br0) and that same address is registered.
LAN_IP=$(ip -4 -o addr show br0 2>/dev/null | awk '{print $4}' | cut -d/ -f1 | head -1)
[ -n "$LAN_IP" ] || LAN_IP=$(ifconfig br0 2>/dev/null | awk '/inet addr/{gsub("addr:","",$2); print $2}' | head -1)
[ -n "$LAN_IP" ] || die "cannot detect the LAN address (br0)"
log "LAN address: $LAN_IP"

arch=$(opkg print-architecture | awk '!/all|noarch/ {print $2}' | head -1)
case "$arch" in
    aarch64*) goarch=arm64 ;;
    mipsel*)  goarch=mipsle ;;
    mips*)    goarch=mips ;;
    *)        die "unsupported Entware architecture: $arch" ;;
esac
log "architecture: $arch -> doqd-linux-$goarch"

if [ "$1" = "--local" ]; then
    [ -f "$2" ] || die "file $2 not found"
    cp "$2" "$BIN.new"
else
    base="https://github.com/$REPO/releases/latest/download"
    log "downloading doqd-linux-$goarch"
    fetch "$base/doqd-linux-$goarch" "$BIN.new" \
        || die "download failed (no internet? private repo? use --local)"
    if command -v sha256sum >/dev/null 2>&1; then
        fetch "$base/SHA256SUMS" /opt/tmp/doqd.sums || die "SHA256SUMS download failed"
        want=$(awk -v f="doqd-linux-$goarch" '$2==f{print $1}' /opt/tmp/doqd.sums)
        got=$(sha256sum "$BIN.new" | awk '{print $1}')
        rm -f /opt/tmp/doqd.sums
        [ -n "$want" ] && [ "$got" = "$want" ] || die "SHA256 mismatch — corrupted download"
        log "SHA256 OK"
    else
        log "WARNING: sha256sum not found, skipping checksum verification"
    fi
fi
chmod 755 "$BIN.new"
"$BIN.new" -version >/dev/null || die "binary does not run (wrong architecture?)"

[ -x "$INIT" ] && "$INIT" stop >/dev/null 2>&1 || true
mv "$BIN.new" "$BIN"

# Existing config is preserved on reinstall/upgrade.
if [ ! -f "$CONF" ]; then
    cat > "$CONF" <<EOF
# doqd — DNS-over-QUIC forwarder. https://github.com/necronicle/keenetic-doq
listen $LAN_IP:$PORT

# DoQ upstreams, in order of preference. Manage with: doqd add / doqd remove
upstream quic://dns.comss.one
upstream quic://unfiltered.adguard-dns.com

# cache: max entries / TTL bounds in seconds
cache_size 4096
min_ttl 60
max_ttl 86400

# log level: debug | info | warn | error
log info
EOF
    log "config written: $CONF"
fi

cat > "$INIT" <<'EOF'
#!/bin/sh

ENABLED=yes
PROCS=doqd
ARGS="-c /opt/etc/doqd.conf"
PREARGS=""
DESC="DNS-over-QUIC forwarder"
PATH=/opt/sbin:/opt/bin:/usr/sbin:/usr/bin:/sbin:/bin

. /opt/etc/init.d/rc.func
EOF
chmod 755 "$INIT"

# Register exactly the address doqd listens on per the config.
NS=$(awk '/^listen /{print $2}' "$CONF")
[ -n "$NS" ] || NS="$LAN_IP:$PORT"

"$INIT" start
sleep 1
"$INIT" check | grep -q alive || die "doqd did not start, check the logs"

log "registering name-server $NS in KeeneticOS"
ndm_cmd "ip name-server $NS"
ndm_cmd "system configuration save"

log "done. Useful commands:"
log "  doqd status                       # health check"
log "  doqd list                         # upstreams with live probes"
log "  doqd add quic://dns.example.com   # add your own DoQ upstream"
```

- [ ] **Step 2: Написать новый uninstall.sh в корне**

```sh
#!/bin/sh
# keenetic-doq uninstaller. Run ON THE ROUTER.
# Unregisters the name-server, stops and removes the daemon.
# The config /opt/etc/doqd.conf is kept (delete it manually if unwanted).
set -e

log() { echo "[keenetic-doq] $*"; }

ndm_cmd() {
    if command -v ndmc >/dev/null 2>&1; then ndmc -c "$1"
    elif command -v ndmq >/dev/null 2>&1; then ndmq -p "$1"
    else echo "[keenetic-doq] run manually in the router CLI: $1"; fi
}

NS=$(awk '/^listen /{print $2}' /opt/etc/doqd.conf 2>/dev/null) || true

if [ -n "$NS" ]; then
    log "unregistering name-server $NS"
    ndm_cmd "no ip name-server $NS"
    ndm_cmd "system configuration save"
else
    log "config not found — unregister manually: no ip name-server <address>"
fi

[ -x /opt/etc/init.d/S56doqd ] && /opt/etc/init.d/S56doqd stop || true
rm -f /opt/etc/init.d/S56doqd /opt/sbin/doqd
log "config /opt/etc/doqd.conf kept (delete manually if unwanted)"
log "done"
```

- [ ] **Step 3: Удалить deploy/, обновить release-workflow**

```bash
git rm -r deploy/
```

В `.github/workflows/release.yml` после шага `build` добавить:

```yaml
      - name: checksums
        run: cd dist && sha256sum doqd-linux-* > SHA256SUMS
```

(шаг `softprops/action-gh-release@v2` с `files: dist/*` подхватит SHA256SUMS автоматически).

- [ ] **Step 4: Синтаксическая проверка скриптов**

Run: `sh -n install.sh && sh -n uninstall.sh && grep -rn "deploy/" --include="*.go" --include="*.md" --include="*.yml" . | grep -v docs/superpowers || true`
Expected: `sh -n` молчит; grep показывает только README.md (старые пути — обновятся в Task 10) и ничего в коде/CI. Если ci.yml или тесты ссылаются на deploy/ — поправить здесь же. Также поправить ссылку `deploy/doqd.conf` в шагах задач: с этого момента для локальных проверок использовать `install.sh`-шаблон не нужно — тестовые конфиги создаются во временных файлах.

- [ ] **Step 5: Commit**

```bash
git add install.sh uninstall.sh .github/workflows/release.yml
git commit -m "feat: self-contained one-command installer, SHA256SUMS in releases"
```

---

### Task 9: LICENSE, CHANGELOG, шаблоны issues, метаданные репозитория

**Files:**
- Create: `LICENSE`, `CHANGELOG.md`, `.github/ISSUE_TEMPLATE/bug_report.md`, `.github/ISSUE_TEMPLATE/feature_request.md`

**Interfaces:**
- Consumes: ничего.
- Produces: обвязку публичного репозитория; версия v0.2.0 в CHANGELOG помечена как Unreleased до Task 12.

- [ ] **Step 1: LICENSE (полный текст GPL-3.0)**

```bash
curl -fsSL https://www.gnu.org/licenses/gpl-3.0.txt -o LICENSE
head -3 LICENSE   # убедиться: "GNU GENERAL PUBLIC LICENSE / Version 3"
```

- [ ] **Step 2: CHANGELOG.md**

```markdown
# Changelog

Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
версии — [SemVer](https://semver.org/lang/ru/).

## [Unreleased]

### Added
- CLI-утилита управления в том же бинарнике: `doqd list`, `doqd test`,
  `doqd add`, `doqd remove`, `doqd status`. Апстримы добавляются с живой
  DoQ-проверкой до записи в конфиг; демон перезапускается автоматически.
- Установка одной командой: самодостаточный `install.sh` в корне
  репозитория (шаблоны конфига и init-скрипта встроены).
- Проверка SHA256 скачанного бинарника при установке; `SHA256SUMS`
  публикуется в каждом релизе.
- Лицензия GPL-3.0, шаблоны issues, английский README (`README.en.md`).

### Changed
- Каталог `deploy/` упразднён: `install.sh`/`uninstall.sh` живут в корне.
- Сообщения инсталлера и утилиты — на английском.

## [0.1.0] — 2026-08-24

### Added
- Демон `doqd`: DNS-форвардер с апстримами DNS-over-QUIC (RFC 9250) —
  живые QUIC-соединения, ID=0 на проводе, кеш ответов (TTL + LRU),
  несколько апстримов с EWMA-выбором быстрейшего, failover и фоновым
  health-check.
- Интеграция с KeeneticOS рядом со штатным DNS: слушатель на
  `<LAN-IP>:5354`, регистрация через `ip name-server`; порт 53 и
  `ndnproxy` не затрагиваются.
- Установка на Entware: init-скрипт `S56doqd`, инсталлер с определением
  архитектуры (aarch64 / mipsel / mips).
- CI: тесты и кросс-сборка; релизы со статическими бинарниками.
```

- [ ] **Step 3: Шаблоны issues**

`.github/ISSUE_TEMPLATE/bug_report.md`:

```markdown
---
name: Bug report
about: doqd не работает или работает не так
---

**Модель роутера и версия KeeneticOS:**

**Архитектура Entware** (`opkg print-architecture`):

**Версия doqd** (`doqd -version`):

**Вывод `doqd status`:**

```
(вставьте сюда)
```

**Что делали и что ожидали:**

**Логи** (`/opt/var/log/` или вывод `doqd -c /opt/etc/doqd.conf` вручную):
```

`.github/ISSUE_TEMPLATE/feature_request.md`:

```markdown
---
name: Feature request
about: Предложение по улучшению
---

**Какую задачу хотите решить:**

**Как это видите:**

**Почему это нельзя сделать текущими средствами:**
```

- [ ] **Step 4: Метаданные репозитория**

```bash
gh repo edit necronicle/keenetic-doq \
  --description "DNS-over-QUIC (RFC 9250) for Keenetic routers — alongside stock DoT/DoH, port 53 untouched" \
  --add-topic keenetic --add-topic dns --add-topic doq --add-topic dns-over-quic \
  --add-topic entware --add-topic router --add-topic quic
```

Expected: команда завершается без ошибок; `gh repo view necronicle/keenetic-doq --json description,repositoryTopics` показывает новые значения.

- [ ] **Step 5: Commit**

```bash
git add LICENSE CHANGELOG.md .github/ISSUE_TEMPLATE/
git commit -m "docs: GPL-3.0 license, changelog, issue templates"
```

---

### Task 10: README RU + EN

**Files:**
- Modify: `README.md` (полная переработка, русский)
- Create: `README.en.md` (английский, зеркало структуры)

**Interfaces:**
- Consumes: фактические команды и вывод утилиты (задачи 1–7), однокомандную установку (Task 8).
- Produces: документацию, на которую ссылаются релиз и описание репозитория.

- [ ] **Step 1: Переписать README.md**

Требования к содержанию (структура обязательна, формулировки — по образцу текущего README, он уже хорош и переиспользуется):

1. Шапка: `# keenetic-doq`, строка `[English](README.en.md)`, бейджи
   `![CI](https://github.com/necronicle/keenetic-doq/actions/workflows/ci.yml/badge.svg)` и
   `![Release](https://img.shields.io/github/v/release/necronicle/keenetic-doq)`.
2. Что это: DoQ рядом со штатными DoT/DoH; сохранить ASCII-схему потока из текущего README; явно: порт 53 не трогается, `opkg dns-override` не нужен, соседние Entware-проекты не страдают.
3. Требования: Keenetic с Entware; таблица архитектур aarch64/mipsel/mips → имена бинарников.
4. Установка одной командой:

```sh
curl -fsSL https://raw.githubusercontent.com/necronicle/keenetic-doq/main/install.sh | sh
```

   плюс оффлайн-вариант `--local`, плюс что именно делает инсталлер (5 пунктов из шапки install.sh).
5. Управление апстримами — примеры РЕАЛЬНОГО вывода `doqd list`, `doqd test quic://dns.quad9.net`, `doqd add`, `doqd remove 2`, `doqd status` (взять из прогона Task 11; на этом шаге поставить вывод с мака, обновить в Task 11 при расхождении).
6. Конфигурация: таблица ключей из текущего README (listen/upstream/cache_size/min_ttl/max_ttl/log) + «после ручной правки: `S56doqd restart`; через `doqd add/remove` рестарт автоматический».
7. Проверка работы: dig напрямую в 5354, dig через :53, `ndmc -c 'show ip name-server'`, tcpdump udp/853 — блок из текущего README.
8. FAQ/диагностика: почему comss первым (ТСПУ блокирует AdGuard DoQ/DoT в ряде сетей РФ — `doqd test` покажет); почему порт 5354 (5353 занят avahi); почему LAN-IP (KeeneticOS отвергает loopback); «добавил сервер, а он down» → `doqd list` показывает живость каждого.
9. Удаление: `curl -fsSL https://raw.githubusercontent.com/necronicle/keenetic-doq/main/uninstall.sh | sh`.
10. Сборка из исходников — блок из текущего README.
11. Ограничения: только клиентский DoQ; без фильтрации; doqd слушает только LAN-адрес (наружу не торчит).
12. Лицензия: GPL-3.0.

- [ ] **Step 2: Написать README.en.md**

Полный перевод README.md на английский, та же структура; первая строка — `[Русский](README.md)`.

- [ ] **Step 3: Проверить ссылки и команды**

Run: `grep -n "deploy/" README.md README.en.md || echo clean`
Expected: `clean` — упоминаний упразднённого deploy/ нет; все пути (`install.sh` в корне, `doqd add …`) соответствуют задачам 1–8.

- [ ] **Step 4: Commit**

```bash
git add README.md README.en.md
git commit -m "docs: rewritten README (RU) and English README for the public release"
```

---

### Task 11: Живой тест на роутере

**Files:**
- Create: `docs/superpowers/live-test-2026-08-25.md` (протокол результатов)
- Modify: `README.md`, `README.en.md` (подставить реальный вывод утилиты, если отличается)

**Interfaces:**
- Consumes: всё из задач 1–10; кросс-сборка arm64; доступ к роутеру (см. Global Constraints).
- Produces: подтверждённую работоспособность на живом Keenetic; зафиксированный протокол.

Каждый шаг — записать фактический вывод в протокол. Роутер в конце должен остаться с работающим doqd (продакшен-состояние).

- [ ] **Step 1: Собрать и залить бинарник и скрипты**

```bash
CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -ldflags "-s -w -X main.version=v0.2.0-rc" -o /tmp/doqd-linux-arm64 ./cmd/doqd
scp -O -P 222 /tmp/doqd-linux-arm64 install.sh uninstall.sh root@192.168.1.1:/opt/tmp/
```

(sshpass с паролем из памяти Zapret2 — как в Global Constraints.)

- [ ] **Step 2: Удалить старую установку и поставить новую через install.sh --local**

```bash
ssh ... "sh /opt/tmp/uninstall.sh && sh /opt/tmp/install.sh --local /opt/tmp/doqd-linux-arm64"
```

Expected: uninstall снимает регистрацию и останавливает старый doqd; install находит LAN-IP, ставит бинарник, СОХРАНЯЕТ существующий конфиг (или пишет новый, если его нет), запускает демона (alive), регистрирует name-server, печатает шпаргалку.

- [ ] **Step 3: Прогнать все подкоманды**

```bash
ssh ... "doqd status && doqd list && doqd test quic://dns.quad9.net"
ssh ... "doqd add quic://dns.quad9.net && doqd list"
ssh ... "dig @192.168.1.1 -p 5354 live-test-$RANDOM.example.org +time=5 | grep status"   # с мака
ssh ... "doqd remove quic://dns.quad9.net && doqd list"
ssh ... "doqd remove 1"   # должен ОТКАЗАТЬСЯ только если апстрим остался один; иначе удалить и вернуть обратно через doqd add
```

Expected: status — running/registration present/NOERROR; list — comss alive с RTT; test quad9 — OK ~200 мс; add — проба, запись, рестарт alive; сквозной резолв NOERROR; remove — работает, последний апстрим защищён. ВНИМАНИЕ: не использовать `killall doqd`; конфиг в конце должен вернуться к исходному составу апстримов.

- [ ] **Step 4: Проверить edge-кейсы**

```bash
ssh ... "doqd add quic://dns.comss.one"          # дубликат → отказ, код 1
ssh ... "doqd add quic://no-such-host.invalid"    # не резолвится → отказ с подсказкой --force
ssh ... "doqd frobnicate; echo exit=$?"           # unknown → usage, exit=2
```

- [ ] **Step 5: Зафиксировать протокол и обновить README**

Записать реальные выводы в `docs/superpowers/live-test-2026-08-25.md`; сверить с примерами в README.md/README.en.md, при расхождении — обновить README.

- [ ] **Step 6: Убедиться в продакшен-состоянии роутера**

```bash
ssh ... "doqd status && grep '^upstream' /opt/etc/doqd.conf"
```

Expected: демон running, регистрация на месте, resolve NOERROR, апстримы — исходные (comss + AdGuard unfiltered). Временные файлы в /opt/tmp удалить.

- [ ] **Step 7: Commit**

```bash
git add docs/superpowers/live-test-2026-08-25.md README.md README.en.md
git commit -m "docs: live test protocol on Keenetic aarch64 for v0.2.0"
```

---

### Task 12: Релиз v0.2.0 и публикация

**Files:**
- Modify: `CHANGELOG.md` (`[Unreleased]` → `[0.2.0] — <дата>`)

**Interfaces:**
- Consumes: зелёный CI, пройденный живой тест (Task 11).
- Produces: релиз v0.2.0 с 3 бинарниками + SHA256SUMS; публичный репозиторий.

- [ ] **Step 1: Финализировать CHANGELOG и убедиться, что CI зелёный**

```bash
# заменить "## [Unreleased]" на "## [0.2.0] — 2026-08-25" (фактическая дата)
git add CHANGELOG.md && git commit -m "docs: changelog for v0.2.0"
git push && gh run watch --exit-status
```

- [ ] **Step 2: Тег и релиз**

```bash
git tag v0.2.0 && git push origin v0.2.0
gh run watch --exit-status   # дождаться release-workflow
gh release view v0.2.0 --json assets -q '.assets[].name'
```

Expected: четыре ассета — `doqd-linux-arm64`, `doqd-linux-mipsle`, `doqd-linux-mips`, `SHA256SUMS`.

- [ ] **Step 3: ГЕЙТ — подтверждение пользователя на публикацию**

СТОП. Спросить пользователя: «v0.2.0 выпущен, всё проверено. Переводим репозиторий в public?» Продолжать ТОЛЬКО после явного «да».

- [ ] **Step 4: Публикация и финальная проверка одной команды**

```bash
gh repo edit necronicle/keenetic-doq --visibility public --accept-visibility-change-consequences
```

Затем живая проверка с роутера БЕЗ токена и без --local:

```bash
ssh ... "sh /opt/tmp/uninstall.sh; curl -fsSL https://raw.githubusercontent.com/necronicle/keenetic-doq/main/install.sh | sh"
ssh ... "doqd status"
```

Expected: установка одной командой скачивает релизный бинарник, сверяет SHA256, демон running, регистрация на месте. Это финальная приёмка милстоуна.

- [ ] **Step 5: Обновить память проекта**

Обновить `/Users/mark/.claude/projects/-Library-DOQ/memory/project_keenetic_doq_state.md`: v0.2.0 выпущен, репо публичный, CLI-утилита в бинарнике, install.sh в корне.
